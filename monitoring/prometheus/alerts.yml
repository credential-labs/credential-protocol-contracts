groups:
  - name: credentialprotocol
    rules:
      - alert: HighErrorRate
        expr: rate(credentialprotocol_api_errors_total[5m]) > 0.1
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "High error rate on Credential Protocol API"
          description: "Error rate is {{ $value | humanizePercentage }} over the last 5 minutes."

      - alert: APIDown
        expr: up{job="credentialprotocol-exporter"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Credential Protocol exporter is down"
          description: "The credentialprotocol-exporter has been unreachable for more than 1 minute."

      - alert: ContractPaused
        expr: credentialprotocol_contract_paused == 1
        for: 0m
        labels:
          severity: warning
        annotations:
          summary: "Credential Protocol contract is paused"
          description: "The contract has been paused by an admin. Investigate before unpausing."

      - alert: LowAttestationRate
        expr: credentialprotocol_attestation_success_rate < 0.5
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "Low attestation success rate"
          description: "Attestation success rate is {{ $value | humanizePercentage }}, below 50%."

      - alert: RateLimitSpike
        expr: rate(credentialprotocol_rate_limit_hits_total[5m]) > 5
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "Unusual rate-limit activity"
          description: "Rate limit exceeded {{ $value }} times/s over the last 5 minutes."

      # #587 — Contract health dashboard alerts
      - alert: ContractErrorBurst
        expr: increase(credentialprotocol_api_errors_total[10m]) > 20
        for: 0m
        labels:
          severity: critical
        annotations:
          summary: "Burst of contract errors"
          description: "{{ $value }} errors in the last 10 minutes."

      # #599 — Backup verification alerts
      - alert: BackupMissing
        expr: time() - credentialprotocol_backup_last_success_timestamp > 90000
        for: 0m
        labels:
          severity: critical
        annotations:
          summary: "No successful backup in 25 hours"
          description: "Last successful backup was {{ $value | humanizeDuration }} ago. Expected every 24h."

      - alert: BackupVerificationFailed
        expr: credentialprotocol_backup_verification_status == 0
        for: 0m
        labels:
          severity: critical
        annotations:
          summary: "Backup integrity verification failed"
          description: "The most recent backup failed integrity checks. Restore may not be possible."

      # #846 — Automated performance regression detection
      - alert: QuerySLABreach
        expr: >
          rate(credentialprotocol_query_sla_violations_total[5m]) > 0
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "Query latency exceeded SLA on {{ $labels.operation }}"
          description: "Operation {{ $labels.operation }} is producing SLA violations at {{ $value | humanize }} violations/s."

      - alert: PerformanceRegressionDetected
        expr: credentialprotocol_performance_regression_detected == 1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Performance regression on {{ $labels.operation }}"
          description: "p95 latency for {{ $labels.operation }} is ≥20% above recorded baseline. Investigate recent deployments."

      - alert: HighP95Latency
        expr: >
          histogram_quantile(0.95,
            rate(credentialprotocol_contract_invocation_duration_seconds_bucket[10m])
          ) > on(operation) credentialprotocol_query_sla_threshold_seconds
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "p95 latency for {{ $labels.operation }} exceeds SLA"
          description: "p95={{ $value | humanizeDuration }} exceeds the configured SLA ceiling for {{ $labels.operation }}."
